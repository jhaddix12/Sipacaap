{% extends 'base.html.twig' %}

{% block title %}Proyecto{% endblock %}
{% block body %}
<link rel="stylesheet" href="{{ asset('css/bootstrap.css') }}" />
<link rel="stylesheet" href="{{ asset('css/main.css') }}" />
    <h1>Proyecto</h1>

    <table class="table tablamODIFICADA">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ proyecto.id }}</td>
            </tr>
            <tr>
                <th>NombreProyecto</th>
                <td>{{ proyecto.nombreProyecto }}</td>
            </tr>
            <tr>
                <th>NombreGuia</th>
                <td>{{ proyecto.nombreGuia }}</td>
            </tr>
            <tr>
                <th>Ubicacion</th>
                <td>{{ proyecto.ubicacion }}</td>
            </tr>
            <tr>
                <th>Fecha</th>
                <td>{{ proyecto.fecha ? proyecto.fecha|date('Y-m-d') : '' }}</td>
            </tr>
            <tr>
                <th>Foto</th>
                <td>{{ proyecto.foto }}</td>
            </tr>
            <tr>
                <th>Descripcion</th>
                <td>{{ proyecto.descripcion }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('proyecto_index') }}">Volver a la lista</a>

    <a href="{{ path('proyecto_edit', {'id': proyecto.id}) }}">edit</a>

    {{ include('proyecto/_delete_form.html.twig') }}
    
    <script src="{{asset('js/bootstrap.min.js')}}"></script>
{% endblock %}
